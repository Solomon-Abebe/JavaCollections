package AlgorithmDSA.MyExamQuestions;

import java.util.Arrays;
/*
Write a method that, given an array of integers,
produces a new array where for every even index starting from each index i,
the sum of elements from index 0 to i is added to the original value at the even index,
and for every odd index, the value at the odd index is replaced with the original value at index i.
Return the resulting array
 */

public class Exam1 {
    public static void main(String[] args) {
//        System.out.println(Arrays.toString(answerOne(new int[]{1,2,3,4,5,6})));
//    }
//    static int [] answerOne(int []a){
//        int[]b=new int[a.length];
//        int sum=0;
//        for (int i = 0; i < a.length; i++) {
//            for (int j = i; j < b.length; j++)// I said j=0 but should be j=i
//                if (j % 2 == 0) {// I said i instead of j
//                    sum += a[i];
//                    b[j] = sum;
//                }
//                else b[j] = a[i];
//        }
//        return b;
//
//        System.out.println(Short.SIZE);

    }
}